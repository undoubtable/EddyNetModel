# Weights

This directory is reserved for trained model checkpoints.

Model weights are not included in this repository because they are generated training artifacts and may be large. Please train the corresponding baseline models using the scripts under `models/` to reproduce checkpoints.

The final reproduced metrics are summarized in `results/baseline_metrics.md`, and baseline training logs are provided under `logs/01_baseline_reproduction/`.
